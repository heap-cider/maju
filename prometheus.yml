# Local dev Prometheus config — scrapes maju-relay metrics.
# The relay runs on the host (not in Docker), so we use host.docker.internal.
# Default metrics port is 9102; override with MAJU_METRICS_PORT.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: maju-relay
    static_configs:
      - targets: ["host.docker.internal:9102"]
