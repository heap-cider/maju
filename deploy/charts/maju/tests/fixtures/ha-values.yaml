# HA shape: replicas=3 + Redis + RWO git (object-store-backed; no RWM). Render-only check.
relayUrl: wss://maju.example.com
ownerPubkey: "abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789"
replicaCount: 3
secrets:
  existingSecret: maju-secrets
externalPostgresql:
  url: "postgres://maju:pw@postgres.example.com:5432/maju"
externalRedis:
  url: "redis://:pw@redis.example.com:6379"
s3:
  bucket: "maju-media"
persistence:
  git:
    enabled: true
    accessMode: ReadWriteOnce
    size: 50Gi
podDisruptionBudget:
  enabled: true
  minAvailable: 2
