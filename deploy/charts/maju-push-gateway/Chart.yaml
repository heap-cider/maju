apiVersion: v2
# Published to oci://ghcr.io/heap-cider/maju/charts via push-chart-release/<version>
# branches (see docs/push-gateway-deployment.md, "Gateway chart release").
name: maju-push-gateway
description: Public capability-gated APNs last-hop gateway for Maju
version: 0.2.0
appVersion: "0.1.0"
type: application
