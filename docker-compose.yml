name: maju

services:
  postgres:
    image: postgres:17-alpine
    container_name: maju-postgres
    environment:
      POSTGRES_USER: maju
      POSTGRES_PASSWORD: maju_dev
      POSTGRES_DB: maju
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - maju-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U maju"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.maju.service: "postgres"
      com.maju.env: "dev"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: maju-redis
    ports:
      - "6379:6379"
    networks:
      - maju-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.maju.service: "redis"
      com.maju.env: "dev"
    restart: unless-stopped

  adminer:
    image: adminer:latest
    container_name: maju-adminer
    ports:
      - "8082:8080"
    networks:
      - maju-net
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    deploy:
      resources:
        limits:
          memory: 64m
    labels:
      com.maju.service: "adminer"
      com.maju.env: "dev"
    restart: unless-stopped

  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    container_name: maju-keycloak
    command: start-dev --http-port=8080
    environment:
      KC_DB: dev-mem
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8180:8080"
    networks:
      - maju-net
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/localhost/8080 && echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512m
    labels:
      com.maju.service: "keycloak"
      com.maju.env: "dev"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: maju-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: maju_dev
      MINIO_ROOT_PASSWORD: maju_dev_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    networks:
      - maju-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 256m
    labels:
      com.maju.service: "minio"
      com.maju.env: "dev"
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    container_name: maju-minio-init
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - maju-net
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 maju_dev maju_dev_secret &&
        mc mb --ignore-existing local/maju-media &&
        mc anonymous set none local/maju-media
      "
    labels:
      com.maju.service: "minio-init"
      com.maju.env: "dev"
    restart: "no"

  prometheus:
    image: prom/prometheus:latest
    container_name: maju-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - maju-net
    extra_hosts:
      - "host.docker.internal:host-gateway"
    deploy:
      resources:
        limits:
          memory: 128m
    labels:
      com.maju.service: "prometheus"
      com.maju.env: "dev"
    restart: unless-stopped

volumes:
  postgres-data:
    name: maju-postgres-data
    labels:
      com.maju.volume: "postgres"
  minio-data:
    name: maju-minio-data
    labels:
      com.maju.volume: "minio"
  prometheus-data:
    name: maju-prometheus-data
    labels:
      com.maju.volume: "prometheus"

networks:
  maju-net:
    name: maju-net
    driver: bridge
    labels:
      com.maju.network: "dev"
