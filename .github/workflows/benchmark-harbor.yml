name: Harbor Maju Orchestra

on:
  push:
    branches: [main]
    paths:
      - "benchmarks/harbor-maju-orchestra/**"
      - "benchmarks/maju-dataset/**"
      - ".github/workflows/benchmark-harbor.yml"
  pull_request:
    paths:
      - "benchmarks/harbor-maju-orchestra/**"
      - "benchmarks/maju-dataset/**"
      - ".github/workflows/benchmark-harbor.yml"

permissions:
  contents: read

jobs:
  test:
    name: Python tests and lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Test adapter
        working-directory: benchmarks/harbor-maju-orchestra
        run: |
          python -m pip install --disable-pip-version-check -e ".[dev]"
          pytest -q
          ruff check .
          # The task verifiers live in the sibling benchmarks/maju-dataset, so
          # they need the harness config passed explicitly to stay linted.
          ruff check --config pyproject.toml ../maju-dataset
      - name: Test provisioner
        working-directory: benchmarks/harbor-maju-orchestra/testbed
        run: |
          python -m pip install --disable-pip-version-check -e ".[dev]"
          pytest -q
          ruff check .
